# Dev lokal — copy ke v3/.env (gitignored). docker-compose.yml sudah punya
# default kok_dev_only untuk POSTGRES_PASSWORD kalau berkas ini gak dipakai.
POSTGRES_PASSWORD=kok_dev_only
ENV=dev
LOG_LEVEL=debug
