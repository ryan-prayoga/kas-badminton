// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: leaderboard.sql

package gen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const listScoredGamePlayersForLeaderboard = `-- name: ListScoredGamePlayersForLeaderboard :many
SELECT gp.user_id, g.played_on, gp.side, g.winner_side
FROM games g
JOIN game_players gp ON gp.game_id = g.id
WHERE g.club_id = $1 AND g.deleted_at IS NULL AND g.winner_side IS NOT NULL
  AND ($2::date IS NULL OR g.played_on >= $2)
  AND ($3::date IS NULL OR g.played_on <= $3)
ORDER BY g.played_on, g.id
`

type ListScoredGamePlayersForLeaderboardParams struct {
	ClubID      uuid.UUID   `json:"club_id"`
	SeasonStart pgtype.Date `json:"season_start"`
	SeasonEnd   pgtype.Date `json:"season_end"`
}

type ListScoredGamePlayersForLeaderboardRow struct {
	UserID     uuid.UUID    `json:"user_id"`
	PlayedOn   pgtype.Date  `json:"played_on"`
	Side       GameSide     `json:"side"`
	WinnerSide NullGameSide `json:"winner_side"`
}

// Papan peringkat klub (PLAN.md §8.3, §14 F7) — cuma main yang SUDAH
// dinilai (winner_side terisi) yang ikut kehitung; main tanpa skor
// dilewati begitu saja di domain.LeaderboardOf. Diurutkan per tanggal
// naik supaya rentetan kemenangan (streak) terhitung benar di lapisan Go.
func (q *Queries) ListScoredGamePlayersForLeaderboard(ctx context.Context, arg ListScoredGamePlayersForLeaderboardParams) ([]ListScoredGamePlayersForLeaderboardRow, error) {
	rows, err := q.db.Query(ctx, listScoredGamePlayersForLeaderboard, arg.ClubID, arg.SeasonStart, arg.SeasonEnd)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListScoredGamePlayersForLeaderboardRow{}
	for rows.Next() {
		var i ListScoredGamePlayersForLeaderboardRow
		if err := rows.Scan(
			&i.UserID,
			&i.PlayedOn,
			&i.Side,
			&i.WinnerSide,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
