// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: clubs_settings.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const updateClubSettings = `-- name: UpdateClubSettings :one
UPDATE clubs SET settings = $2, updated_at = now(), version = version + 1
WHERE id = $1 AND version = $3
RETURNING id, slug, name, timezone, status, settings, quotas, merchant_qris, deleted_at, purge_after, created_by, created_at, updated_at, version
`

type UpdateClubSettingsParams struct {
	ID       uuid.UUID `json:"id"`
	Settings []byte    `json:"settings"`
	Version  int64     `json:"version"`
}

// Replace penuh, bukan merge — pemanggil (handler) yang menggabungkan field
// lama+baru sebelum kirim, supaya "sumber kebenaran shape settings" tetap
// satu tempat (defaultClubSettings di clubs.go), bukan dua jalur beda logic.
func (q *Queries) UpdateClubSettings(ctx context.Context, arg UpdateClubSettingsParams) (Club, error) {
	row := q.db.QueryRow(ctx, updateClubSettings, arg.ID, arg.Settings, arg.Version)
	var i Club
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.Timezone,
		&i.Status,
		&i.Settings,
		&i.Quotas,
		&i.MerchantQris,
		&i.DeletedAt,
		&i.PurgeAfter,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}
