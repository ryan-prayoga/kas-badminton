// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: users.sql

package gen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createUser = `-- name: CreateUser :one
INSERT INTO users (phone, display_name, status)
VALUES ($1, $2, 'active')
RETURNING id, phone, username, display_name, photo_id, status, username_changed_at, created_at, updated_at, version
`

type CreateUserParams struct {
	Phone       pgtype.Text `json:"phone"`
	DisplayName string      `json:"display_name"`
}

// Dipanggil dari VerifyOTP (internal/auth/otp.go) begitu nomor terverifikasi
// — langsung 'active' karena OTP SUDAH membuktikan pemilik nomor
// (bukan status 'unclaimed', itu cuma untuk akun bayangan hasil migrasi
// v2 yang belum pernah diverifikasi siapa pun, §7.3).
func (q *Queries) CreateUser(ctx context.Context, arg CreateUserParams) (User, error) {
	row := q.db.QueryRow(ctx, createUser, arg.Phone, arg.DisplayName)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Phone,
		&i.Username,
		&i.DisplayName,
		&i.PhotoID,
		&i.Status,
		&i.UsernameChangedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}

const getUser = `-- name: GetUser :one
SELECT id, phone, username, display_name, photo_id, status, username_changed_at, created_at, updated_at, version FROM users WHERE id = $1
`

func (q *Queries) GetUser(ctx context.Context, id uuid.UUID) (User, error) {
	row := q.db.QueryRow(ctx, getUser, id)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Phone,
		&i.Username,
		&i.DisplayName,
		&i.PhotoID,
		&i.Status,
		&i.UsernameChangedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}

const getUserByPhone = `-- name: GetUserByPhone :one
SELECT id, phone, username, display_name, photo_id, status, username_changed_at, created_at, updated_at, version FROM users WHERE phone = $1
`

func (q *Queries) GetUserByPhone(ctx context.Context, phone pgtype.Text) (User, error) {
	row := q.db.QueryRow(ctx, getUserByPhone, phone)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Phone,
		&i.Username,
		&i.DisplayName,
		&i.PhotoID,
		&i.Status,
		&i.UsernameChangedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}
