// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: games.sql

package gen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createExpense = `-- name: CreateExpense :one
INSERT INTO expenses (club_id, amount, note, recorded_by)
VALUES ($1, $2, $3, $4)
RETURNING id, club_id, amount, kok_type_id, type_name, slops, note, recorded_by, created_at
`

type CreateExpenseParams struct {
	ClubID     uuid.UUID   `json:"club_id"`
	Amount     int64       `json:"amount"`
	Note       pgtype.Text `json:"note"`
	RecordedBy *uuid.UUID  `json:"recorded_by"`
}

// Dipakai buat mencatat kelebihan pembulatan biaya sebagai kas masuk
// (§9.5 aturan A) — amount negatif = kas masuk (DDL.sql baris 303-304).
func (q *Queries) CreateExpense(ctx context.Context, arg CreateExpenseParams) (Expense, error) {
	row := q.db.QueryRow(ctx, createExpense,
		arg.ClubID,
		arg.Amount,
		arg.Note,
		arg.RecordedBy,
	)
	var i Expense
	err := row.Scan(
		&i.ID,
		&i.ClubID,
		&i.Amount,
		&i.KokTypeID,
		&i.TypeName,
		&i.Slops,
		&i.Note,
		&i.RecordedBy,
		&i.CreatedAt,
	)
	return i, err
}

const createGame = `-- name: CreateGame :one
INSERT INTO games (club_id, played_on, format, notes, score_format, winner_side, recorded_by)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, club_id, played_on, format, notes, score_format, winner_side, recorded_by, recorded_by_name, created_at, updated_at, deleted_at, version
`

type CreateGameParams struct {
	ClubID      uuid.UUID       `json:"club_id"`
	PlayedOn    pgtype.Date     `json:"played_on"`
	Format      GameFormat      `json:"format"`
	Notes       pgtype.Text     `json:"notes"`
	ScoreFormat NullScoreFormat `json:"score_format"`
	WinnerSide  NullGameSide    `json:"winner_side"`
	RecordedBy  *uuid.UUID      `json:"recorded_by"`
}

func (q *Queries) CreateGame(ctx context.Context, arg CreateGameParams) (Game, error) {
	row := q.db.QueryRow(ctx, createGame,
		arg.ClubID,
		arg.PlayedOn,
		arg.Format,
		arg.Notes,
		arg.ScoreFormat,
		arg.WinnerSide,
		arg.RecordedBy,
	)
	var i Game
	err := row.Scan(
		&i.ID,
		&i.ClubID,
		&i.PlayedOn,
		&i.Format,
		&i.Notes,
		&i.ScoreFormat,
		&i.WinnerSide,
		&i.RecordedBy,
		&i.RecordedByName,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
		&i.Version,
	)
	return i, err
}

const createGameKok = `-- name: CreateGameKok :one
INSERT INTO game_koks (game_id, club_id, kok_type_id, type_name, price_per_person, qty)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, game_id, club_id, kok_type_id, type_name, price_per_person, qty
`

type CreateGameKokParams struct {
	GameID         uuid.UUID   `json:"game_id"`
	ClubID         uuid.UUID   `json:"club_id"`
	KokTypeID      *uuid.UUID  `json:"kok_type_id"`
	TypeName       pgtype.Text `json:"type_name"`
	PricePerPerson int64       `json:"price_per_person"`
	Qty            int32       `json:"qty"`
}

func (q *Queries) CreateGameKok(ctx context.Context, arg CreateGameKokParams) (GameKok, error) {
	row := q.db.QueryRow(ctx, createGameKok,
		arg.GameID,
		arg.ClubID,
		arg.KokTypeID,
		arg.TypeName,
		arg.PricePerPerson,
		arg.Qty,
	)
	var i GameKok
	err := row.Scan(
		&i.ID,
		&i.GameID,
		&i.ClubID,
		&i.KokTypeID,
		&i.TypeName,
		&i.PricePerPerson,
		&i.Qty,
	)
	return i, err
}

const createGamePlayer = `-- name: CreateGamePlayer :one
INSERT INTO game_players (game_id, club_id, user_id, payer_id, side, slot, amount)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, game_id, club_id, user_id, payer_id, side, slot, amount, paid_at, paid_by, disputed_at, dispute_note
`

type CreateGamePlayerParams struct {
	GameID  uuid.UUID `json:"game_id"`
	ClubID  uuid.UUID `json:"club_id"`
	UserID  uuid.UUID `json:"user_id"`
	PayerID uuid.UUID `json:"payer_id"`
	Side    GameSide  `json:"side"`
	Slot    int16     `json:"slot"`
	Amount  int64     `json:"amount"`
}

func (q *Queries) CreateGamePlayer(ctx context.Context, arg CreateGamePlayerParams) (GamePlayer, error) {
	row := q.db.QueryRow(ctx, createGamePlayer,
		arg.GameID,
		arg.ClubID,
		arg.UserID,
		arg.PayerID,
		arg.Side,
		arg.Slot,
		arg.Amount,
	)
	var i GamePlayer
	err := row.Scan(
		&i.ID,
		&i.GameID,
		&i.ClubID,
		&i.UserID,
		&i.PayerID,
		&i.Side,
		&i.Slot,
		&i.Amount,
		&i.PaidAt,
		&i.PaidBy,
		&i.DisputedAt,
		&i.DisputeNote,
	)
	return i, err
}

const createGameScore = `-- name: CreateGameScore :one
INSERT INTO game_scores (game_id, game_no, score_a, score_b)
VALUES ($1, $2, $3, $4)
RETURNING game_id, game_no, score_a, score_b
`

type CreateGameScoreParams struct {
	GameID uuid.UUID `json:"game_id"`
	GameNo int16     `json:"game_no"`
	ScoreA int16     `json:"score_a"`
	ScoreB int16     `json:"score_b"`
}

func (q *Queries) CreateGameScore(ctx context.Context, arg CreateGameScoreParams) (GameScore, error) {
	row := q.db.QueryRow(ctx, createGameScore,
		arg.GameID,
		arg.GameNo,
		arg.ScoreA,
		arg.ScoreB,
	)
	var i GameScore
	err := row.Scan(
		&i.GameID,
		&i.GameNo,
		&i.ScoreA,
		&i.ScoreB,
	)
	return i, err
}

const getGame = `-- name: GetGame :one
SELECT id, club_id, played_on, format, notes, score_format, winner_side, recorded_by, recorded_by_name, created_at, updated_at, deleted_at, version FROM games WHERE id = $1 AND club_id = $2 AND deleted_at IS NULL
`

type GetGameParams struct {
	ID     uuid.UUID `json:"id"`
	ClubID uuid.UUID `json:"club_id"`
}

func (q *Queries) GetGame(ctx context.Context, arg GetGameParams) (Game, error) {
	row := q.db.QueryRow(ctx, getGame, arg.ID, arg.ClubID)
	var i Game
	err := row.Scan(
		&i.ID,
		&i.ClubID,
		&i.PlayedOn,
		&i.Format,
		&i.Notes,
		&i.ScoreFormat,
		&i.WinnerSide,
		&i.RecordedBy,
		&i.RecordedByName,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
		&i.Version,
	)
	return i, err
}

const listGameKoksByGame = `-- name: ListGameKoksByGame :many
SELECT id, game_id, club_id, kok_type_id, type_name, price_per_person, qty FROM game_koks WHERE game_id = $1
`

func (q *Queries) ListGameKoksByGame(ctx context.Context, gameID uuid.UUID) ([]GameKok, error) {
	rows, err := q.db.Query(ctx, listGameKoksByGame, gameID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GameKok{}
	for rows.Next() {
		var i GameKok
		if err := rows.Scan(
			&i.ID,
			&i.GameID,
			&i.ClubID,
			&i.KokTypeID,
			&i.TypeName,
			&i.PricePerPerson,
			&i.Qty,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGamePlayersByGame = `-- name: ListGamePlayersByGame :many
SELECT id, game_id, club_id, user_id, payer_id, side, slot, amount, paid_at, paid_by, disputed_at, dispute_note FROM game_players WHERE game_id = $1 ORDER BY side, slot
`

func (q *Queries) ListGamePlayersByGame(ctx context.Context, gameID uuid.UUID) ([]GamePlayer, error) {
	rows, err := q.db.Query(ctx, listGamePlayersByGame, gameID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GamePlayer{}
	for rows.Next() {
		var i GamePlayer
		if err := rows.Scan(
			&i.ID,
			&i.GameID,
			&i.ClubID,
			&i.UserID,
			&i.PayerID,
			&i.Side,
			&i.Slot,
			&i.Amount,
			&i.PaidAt,
			&i.PaidBy,
			&i.DisputedAt,
			&i.DisputeNote,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGamePlayersByGames = `-- name: ListGamePlayersByGames :many
SELECT id, game_id, club_id, user_id, payer_id, side, slot, amount, paid_at, paid_by, disputed_at, dispute_note FROM game_players WHERE game_id = ANY($1::uuid[]) ORDER BY side, slot
`

func (q *Queries) ListGamePlayersByGames(ctx context.Context, gameIds []uuid.UUID) ([]GamePlayer, error) {
	rows, err := q.db.Query(ctx, listGamePlayersByGames, gameIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GamePlayer{}
	for rows.Next() {
		var i GamePlayer
		if err := rows.Scan(
			&i.ID,
			&i.GameID,
			&i.ClubID,
			&i.UserID,
			&i.PayerID,
			&i.Side,
			&i.Slot,
			&i.Amount,
			&i.PaidAt,
			&i.PaidBy,
			&i.DisputedAt,
			&i.DisputeNote,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGamesByClub = `-- name: ListGamesByClub :many
SELECT id, club_id, played_on, format, notes, score_format, winner_side, recorded_by, recorded_by_name, created_at, updated_at, deleted_at, version FROM games
WHERE club_id = $1
  AND deleted_at IS NULL
  AND ($3::date IS NULL OR played_on < $3::date)
ORDER BY played_on DESC, id DESC
LIMIT $2
`

type ListGamesByClubParams struct {
	ClubID uuid.UUID   `json:"club_id"`
	Limit  int32       `json:"limit"`
	Before pgtype.Date `json:"before"`
}

// Cursor per tanggal (§4.2 "daftar main dimuat per bulan"; API.md §0
// paginasi cursor, bukan offset). sqlc.narg(before) NULL = dari yang
// terbaru.
func (q *Queries) ListGamesByClub(ctx context.Context, arg ListGamesByClubParams) ([]Game, error) {
	rows, err := q.db.Query(ctx, listGamesByClub, arg.ClubID, arg.Limit, arg.Before)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Game{}
	for rows.Next() {
		var i Game
		if err := rows.Scan(
			&i.ID,
			&i.ClubID,
			&i.PlayedOn,
			&i.Format,
			&i.Notes,
			&i.ScoreFormat,
			&i.WinnerSide,
			&i.RecordedBy,
			&i.RecordedByName,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
			&i.Version,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const softDeleteGame = `-- name: SoftDeleteGame :one
UPDATE games
SET deleted_at = now(), updated_at = now(), version = version + 1
WHERE id = $1 AND club_id = $2 AND deleted_at IS NULL
RETURNING id, club_id, played_on, format, notes, score_format, winner_side, recorded_by, recorded_by_name, created_at, updated_at, deleted_at, version
`

type SoftDeleteGameParams struct {
	ID     uuid.UUID `json:"id"`
	ClubID uuid.UUID `json:"club_id"`
}

func (q *Queries) SoftDeleteGame(ctx context.Context, arg SoftDeleteGameParams) (Game, error) {
	row := q.db.QueryRow(ctx, softDeleteGame, arg.ID, arg.ClubID)
	var i Game
	err := row.Scan(
		&i.ID,
		&i.ClubID,
		&i.PlayedOn,
		&i.Format,
		&i.Notes,
		&i.ScoreFormat,
		&i.WinnerSide,
		&i.RecordedBy,
		&i.RecordedByName,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
		&i.Version,
	)
	return i, err
}

const sumUnpaidByPayer = `-- name: SumUnpaidByPayer :one
SELECT COALESCE(SUM(amount), 0)::bigint AS total
FROM game_players
WHERE club_id = $1 AND payer_id = $2 AND paid_at IS NULL AND disputed_at IS NULL
`

type SumUnpaidByPayerParams struct {
	ClubID  uuid.UUID `json:"club_id"`
	PayerID uuid.UUID `json:"payer_id"`
}

// Query "tagihanku" — satu index scan lewat game_players_unpaid_idx
// (indeks paling penting di seluruh skema, DDL.sql baris 371-379).
func (q *Queries) SumUnpaidByPayer(ctx context.Context, arg SumUnpaidByPayerParams) (int64, error) {
	row := q.db.QueryRow(ctx, sumUnpaidByPayer, arg.ClubID, arg.PayerID)
	var total int64
	err := row.Scan(&total)
	return total, err
}

const updateGame = `-- name: UpdateGame :one
UPDATE games
SET notes = $3, updated_at = now(), version = version + 1
WHERE id = $1 AND club_id = $2 AND version = $4 AND deleted_at IS NULL
RETURNING id, club_id, played_on, format, notes, score_format, winner_side, recorded_by, recorded_by_name, created_at, updated_at, deleted_at, version
`

type UpdateGameParams struct {
	ID      uuid.UUID   `json:"id"`
	ClubID  uuid.UUID   `json:"club_id"`
	Notes   pgtype.Text `json:"notes"`
	Version int64       `json:"version"`
}

// Versioning (invarian §3.4): 0 baris kalau version klien sudah basi —
// caller lalu fetch entitas terbaru dan balas 409 version_conflict.
func (q *Queries) UpdateGame(ctx context.Context, arg UpdateGameParams) (Game, error) {
	row := q.db.QueryRow(ctx, updateGame,
		arg.ID,
		arg.ClubID,
		arg.Notes,
		arg.Version,
	)
	var i Game
	err := row.Scan(
		&i.ID,
		&i.ClubID,
		&i.PlayedOn,
		&i.Format,
		&i.Notes,
		&i.ScoreFormat,
		&i.WinnerSide,
		&i.RecordedBy,
		&i.RecordedByName,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
		&i.Version,
	)
	return i, err
}
