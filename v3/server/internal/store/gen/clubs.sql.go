// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: clubs.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const createClub = `-- name: CreateClub :one
INSERT INTO clubs (id, slug, name, timezone, settings, quotas, created_by)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, slug, name, timezone, status, settings, quotas, merchant_qris, deleted_at, purge_after, created_by, created_at, updated_at, version
`

type CreateClubParams struct {
	ID        uuid.UUID  `json:"id"`
	Slug      string     `json:"slug"`
	Name      string     `json:"name"`
	Timezone  string     `json:"timezone"`
	Settings  []byte     `json:"settings"`
	Quotas    []byte     `json:"quotas"`
	CreatedBy *uuid.UUID `json:"created_by"`
}

// id dikirim eksplisit (bukan DEFAULT gen_random_uuid()) supaya pemanggil
// tahu club_id SEBELUM transaksi mulai — dipakai buat SET LOCAL
// app.club_id di store.WithClub yang sama, jadi insert clubs + membership
// admin pembuatnya atomik dalam satu transaksi (internal/http/clubs.go).
func (q *Queries) CreateClub(ctx context.Context, arg CreateClubParams) (Club, error) {
	row := q.db.QueryRow(ctx, createClub,
		arg.ID,
		arg.Slug,
		arg.Name,
		arg.Timezone,
		arg.Settings,
		arg.Quotas,
		arg.CreatedBy,
	)
	var i Club
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.Timezone,
		&i.Status,
		&i.Settings,
		&i.Quotas,
		&i.MerchantQris,
		&i.DeletedAt,
		&i.PurgeAfter,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}

const getClub = `-- name: GetClub :one
SELECT id, slug, name, timezone, status, settings, quotas, merchant_qris, deleted_at, purge_after, created_by, created_at, updated_at, version FROM clubs WHERE id = $1 AND deleted_at IS NULL
`

// Tanpa club_id di WHERE — clubs tidak ber-RLS (tabel dirinya sendiri
// bukan milik klub). Caller (RequireClub) sudah memverifikasi keanggotaan
// lewat GetMembership sebelum ini dipanggil.
func (q *Queries) GetClub(ctx context.Context, id uuid.UUID) (Club, error) {
	row := q.db.QueryRow(ctx, getClub, id)
	var i Club
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.Timezone,
		&i.Status,
		&i.Settings,
		&i.Quotas,
		&i.MerchantQris,
		&i.DeletedAt,
		&i.PurgeAfter,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}

const getClubBySlug = `-- name: GetClubBySlug :one
SELECT id, slug, name, timezone, status, settings, quotas, merchant_qris, deleted_at, purge_after, created_by, created_at, updated_at, version FROM clubs WHERE slug = $1 AND deleted_at IS NULL
`

func (q *Queries) GetClubBySlug(ctx context.Context, slug string) (Club, error) {
	row := q.db.QueryRow(ctx, getClubBySlug, slug)
	var i Club
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.Timezone,
		&i.Status,
		&i.Settings,
		&i.Quotas,
		&i.MerchantQris,
		&i.DeletedAt,
		&i.PurgeAfter,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}
