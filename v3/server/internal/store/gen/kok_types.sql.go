// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: kok_types.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const countKokTypesByClub = `-- name: CountKokTypesByClub :one
SELECT count(*) FROM kok_types WHERE club_id = $1
`

// Dipakai internal/store/tenant_test.go — sengaja query dengan club_id
// eksplisit yang BEDA dari SET LOCAL app.club_id aktif, buat membuktikan
// RLS tetap menahan meski filter aplikasinya "benar" (§6.2 lapisan
// ketiga).
func (q *Queries) CountKokTypesByClub(ctx context.Context, clubID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countKokTypesByClub, clubID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createKokType = `-- name: CreateKokType :one
INSERT INTO kok_types (club_id, name, price_per_person)
VALUES ($1, $2, $3)
RETURNING id, club_id, name, price_per_person, price_per_slop, stock, active, created_at, updated_at, version
`

type CreateKokTypeParams struct {
	ClubID         uuid.UUID `json:"club_id"`
	Name           string    `json:"name"`
	PricePerPerson int64     `json:"price_per_person"`
}

// Dipakai cmd/seed buat data contoh (CRUD katalog sendiri di luar F2).
func (q *Queries) CreateKokType(ctx context.Context, arg CreateKokTypeParams) (KokType, error) {
	row := q.db.QueryRow(ctx, createKokType, arg.ClubID, arg.Name, arg.PricePerPerson)
	var i KokType
	err := row.Scan(
		&i.ID,
		&i.ClubID,
		&i.Name,
		&i.PricePerPerson,
		&i.PricePerSlop,
		&i.Stock,
		&i.Active,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}

const getKokType = `-- name: GetKokType :one
SELECT id, club_id, name, price_per_person, price_per_slop, stock, active, created_at, updated_at, version FROM kok_types WHERE id = $1 AND club_id = $2 AND active
`

type GetKokTypeParams struct {
	ID     uuid.UUID `json:"id"`
	ClubID uuid.UUID `json:"club_id"`
}

// Dipakai POST games buat snapshot harga (§8 "harga & nama dikunci di
// dalam game, tidak pernah diambil ulang dari katalog"). CRUD katalog
// kok_types sendiri di luar lingkup F2 (slice sempit clubs+games) — kalau
// kok_type_id tidak dikirim, klien boleh kirim type_name+price_per_person
// ad-hoc (kolom kok_type_id di game_koks memang nullable).
func (q *Queries) GetKokType(ctx context.Context, arg GetKokTypeParams) (KokType, error) {
	row := q.db.QueryRow(ctx, getKokType, arg.ID, arg.ClubID)
	var i KokType
	err := row.Scan(
		&i.ID,
		&i.ClubID,
		&i.Name,
		&i.PricePerPerson,
		&i.PricePerSlop,
		&i.Stock,
		&i.Active,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Version,
	)
	return i, err
}
